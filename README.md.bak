# TrendRadar 定制分支

本分支基于 [sansan0/TrendRadar](https://github.com/sansan0/TrendRadar) v6.10.0。本页只记录与上游的差异，通用功能和部署教程请参阅 [上游文档](https://trendradar.sandev.cc/zh/docs/quick-start/)。

- **数据分支持久化**

  GitHub Actions 使用本地 SQLite，运行前从独立的 `data` 分支恢复数据库，运行后保存热榜、RSS 和推荐状态，无需配置 S3。推荐状态保存在 `output/state.db`。

  默认保留最近 30 天的数据，首次运行清理过期数据，此后每 30 天清理一次，分别由 `storage.local.retention_days` 和 `storage.local.cleanup_interval_days` 控制。清理不删除 Git 历史中的快照。恢复方式与失败处理见 [数据分支说明](docs/data-branch.md)。

- **跨天去重与失败重试**

  默认报告模式由 `current` 改为 `incremental`，并启用 `report.cross_day_dedup`。热榜和 RSS 按来源及规范化链接去重，追踪参数或标题变化不会让同一链接重复推荐，去重窗口跟随数据保留天数。

  待推荐内容先持久化；所有通知渠道发送失败时保留到下次重试，文章离榜或跨天后仍可重试。任一渠道成功即确认本次推荐；仅使用网页时，以报告生成成功为准。跨天去重仅作用于增量模式，依赖本地状态数据库。

- **网页首页与发布**

  最新完整报告保存为 `data` 分支根目录的 `index.html`，代码分支不再跟踪生成的首页；没有新报告时保留上一版。默认首页展示最近一批增量推荐。

  GitHub Pages 由工作流显式部署已保存的首页，发布包只包含网页。启用时在 **Settings → Pages → Build and deployment → Source** 选择 **GitHub Actions**，再运行一次 **Get Hot News**。Cloudflare Pages 同样使用保存成功的首页。

- **推送与报告格式**

  网页和各通知渠道统一采用“标题在前，来源、排名和时间在后”的结构，并区分三种模式：当日汇总显示最高排名与采集命中次数，当前榜单显示当前排名及可判断的涨跌，增量更新显示本次排名与首次发现时间。统计区分展示条数与匹配条数。

  长通知优先按完整新闻拆分，续条保留页码、分组和原编号。通用 Webhook 新增 `notification.channels.generic_webhook.format`，支持 `markdown` 和 `text`，也可通过 `GENERIC_WEBHOOK_FORMAT` 环境变量或 Actions Secret 设置。示例与各渠道格式见 [推送格式说明](docs/notification-format.md)。

- **关键词匹配规则**

  `!过滤词` 改为仅排除所在词组，不影响其他词组；`[GLOBAL_FILTER]` 支持普通词、正则表达式和别名。热榜、RSS 和 MCP 共用匹配逻辑，统一处理必须词、组内排除与全局过滤。

  新增 [词库校验脚本](scripts/validate_frequency_words.py) 和 [历史新闻回放脚本](scripts/audit_frequency_words.py)，用于检查配置结构、重复规则、正则语法，以及分组命中、过滤样本和零命中规则。回放需要本地新闻数据库。

- **默认数据源与词库**

  [中文配置](config/config.yaml) 扩充科技、开发者、安全和国际资讯来源，例如 Hacker News、掘金、少数派、Solidot、IT之家、Freebuf，以及 CnBeta、南华早报、纽约时报、半岛电视台等 RSS。

  [默认词库](config/frequency_words.txt) 按主题重写，同组维护中英文规则，为英文短词增加边界约束，并用更具体的营销话术规则替换宽泛过滤词。[自定义关键词](config/custom/keyword/README.md) 自动与默认词库合并，人物示例见 [Total.txt](config/custom/keyword/Total.txt)。

- **自动化工作流**

  [爬虫工作流](.github/workflows/crawler.yml) 的定时表达式由 `33 * * * *` 改为 `0 * * * *`（每小时整点），签到试用阈值由 7 天延长至 7000 天。爬虫任务共用 `trendradar-data` 并发组，运行中的任务不再被新任务取消。

  新增 [词库校验](.github/workflows/frequency-words.yml) 和 [数据库及相关回归测试](.github/workflows/database-tests.yml) 工作流。
