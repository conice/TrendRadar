# 自定义关键词配置

这里存放个人关注或带时效性的补充词表。默认加载 `config/frequency_words.txt`
时，会自动合并本目录下的所有 `.txt` 文件，调度未启用时也生效。

[Total.txt](Total.txt) 已提供人物示例：马斯克、黄仁勋、雷军和山姆·奥尔特曼，
包含中文姓名和英文姓名，每人最多显示 5 条新闻。可以直接修改、增加或删除人物组。

合并规则：

- 自定义文件按文件名排序，文件内保持词组顺序；自定义词组排在默认词组前。
  同一标题同时命中人物和通用主题时，优先归入人物组。
- 每个词组的必须词 `+`、组内过滤词 `!` 和数量上限 `@` 独立生效。
  完全相同的词组及全局过滤规则只保留一份。
- 默认词库和自定义词表的 `[GLOBAL_FILTER]` 共同生效，过滤所有词组。
- 自定义文件只需包含 `[WORD_GROUPS]` 和新增词组；没有额外全局过滤词时，
  可以省略 `[GLOBAL_FILTER]`，无需复制默认词库的内容。
- 只读取本目录直属的 `.txt` 文件。停用一个文件时，将它移出本目录或改成 `.txt.disabled`。

例如在 `Total.txt` 中追加一个人物，组与组之间保留空行：

```text
[苏姿丰]
苏姿丰
/(?<![A-Za-z0-9_])Lisa[\s-]+Su(?![A-Za-z0-9_])/ => Lisa Su
@5
```

英文正则自动忽略大小写，上例同时允许空格和连字符；边界约束避免误匹配更长的英文词。

如需在某个时段只合并一个自定义文件，可在已启用的 `config/timeline.yaml` 预设的
`default` 或时间段中设置：

```yaml
frequency_file: "Total.txt"
```

此时加载「默认词库 + Total.txt」，其他自定义文件不参与该时段的合并。
`FREQUENCY_WORDS_PATH=config/custom/keyword/Total.txt` 也可选择该文件。

建议在文件头记录：

```text
# Owner: your-name
# Last-Reviewed: 2026-09-07
# Expires: 2026-12-31
```

长期关注可省略 `Expires`。时效组应设置较小的数量上限，并在到期后删除或续期；
日期注释用于人工维护，程序不会自动停用过期词组。

修改后运行：

```sh
python scripts/validate_frequency_words.py config/custom/keyword/Total.txt --strict
python scripts/audit_frequency_words.py --frequency-file config/custom/keyword/Total.txt
```

校验只检查指定文件；历史回放使用合并后的规则，需要本地新闻数据库。
